import { 
  StyleSheet
} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FEFC03',
    //alignItems: 'center',
    //justifyContent: 'center',
  },
});
